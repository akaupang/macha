name: test
channels:
  - conda-forge
  - defaults
dependencies:
  # Base depends
  - parmed
  - openbabel
    # Testing
  - pytest
  - pytest-cov
  - codecov
  - pip:
    - git+https://github.com/ParmEd/ParmEd.git
