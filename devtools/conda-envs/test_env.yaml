name: test
channels:
  - conda-forge
  - defaults
dependencies:
  # Base depends
  - openbabel
    # Testing
  - pytest
  - pytest-cov
  - codecov
  - pip:
    - git+https://github.com/ParmEd/ParmEd.git # Version which should support LPs
